package tokenizer;

public enum TokenType 
{
    TYPE,
    LITERAL_INT,
    LITERAL_CHAR,

    IDENTIFIER, 
    EXIT,

    PLUS,
    MINUS,
    TIMES,
    DIVISION,
    MOD,
    OPEN_PAREN,
    CLOSE_PAREN,
    EQUALS,
    SEMICOLON,
    SINGLE_QUOTE
}
